"""Command-line interface modules."""

from __future__ import annotations

# CLI modules don't need to expose anything at package level
# They are accessed via console scripts defined in pyproject.toml